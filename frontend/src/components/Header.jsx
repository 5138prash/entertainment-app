import React from 'react'  // Importing the React library to create the component

const Header = () => {
  return (
    // The 'header' element contains the main title of the app
    <header className='text-[32px] font-semibold text-center'>
        {/* The title of the app, displayed in the center of the header */}
        Entertainment App  
    </header>
  )
}

export default Header  // Exporting the Header component so it can be used in other parts of the app
