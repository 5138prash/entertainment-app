// Base URL for fetching images from TMDB (The Movie Database) API with a width of 500px
export const IMAGE_BASE_URL = "https://image.tmdb.org/t/p/w500";

// Base URL for your API, typically for backend requests (currently empty for your setup)
export const BASE_URL = "https://entertainment-app-jswn.onrender.com/";

// Endpoint for home-related data (e.g., home page details)
export const HOME_URL = '/api/home';

// Endpoint for fetching movies data
export const MOVIES_URL = '/api/movies';

// Endpoint for fetching TV series data
export const TVSERIES_URL = '/api/tvseries';

// Endpoint for user-related actions (authentication, user data, etc.)
export const USERS_URL = '/api/users';

// Endpoint for managing bookmarks (add, delete, etc.)
export const BOOKMARKS_URL = '/api/bookmarks';
